  <div class="container">
    <div class="row">
      <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-3">
        <%= simple_form_for @car do |f| %>
          <%= f.error_notification %>
          <%= f.input :brand, collection: Car::CATEGORIES, prompt: "Selectionner la marque", label: false %>
          <%= f.input :model, label: 'Modèle' %>
          <%= f.input :address, label: 'Adresse' %>
          <%= f.input :price_cents, label: 'Prix par jour' %>
          <%= f.input :description %>
          <%= f.input :engin, collection: ['Essence', 'Diesel'], label: 'Type de moteur :' %>
          <%= f.input :photo, as: :attachinary %>
          <span class="flex padded-top padded-bot"><%= f.button :submit, "#{@submit_label}", class: "btn btn-primary" %></span>
        <% end %>
      </div>
    </div>
  </div>
